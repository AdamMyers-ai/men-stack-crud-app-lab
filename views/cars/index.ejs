<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head.ejs") %>

  <body>
    <%- include("../partials/navbar.ejs") %>

    <h1>All Cars</h1>

    <ul>
      <% cars.forEach((car) => { %>
      <li>
        <a href="/cars/<%= car._id %>"><%= car.name %></a>
      </li>
      <% }); %>
    </ul>
  </body>
</html>
